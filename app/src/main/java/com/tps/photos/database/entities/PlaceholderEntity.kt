package com.tps.photos.database.entities

import androidx.room.Entity
import androidx.room.PrimaryKey

/**
 * A placeholder entity.
 */
@Entity
data class PlaceholderEntity(
    @PrimaryKey val id: String,
)
